// configuracion de conexion del cliente web
// este es el unico lugar donde se define a que backend apunta el frontend
// para alternar entre la vm y local solo se cambia el valor activo de API_BASE
// reglas: API_BASE no debe terminar en barra

// vm de oracle controlada por javier
window.API_BASE = 'http://170.9.26.131:80';

// local en este equipo (xampp), ajusta la subcarpeta si difiere
// window.API_BASE = 'http://localhost/sistema_entregas';
